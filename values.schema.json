{
  "$schema": "https://json-schema.org/draft-07/schema#",
  "title": "nuc-certificates values",
  "description": "Schema for nuc-certificates Helm chart values.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "nameOverride": {
      "type": "string",
      "description": "Override the chart name used in generated labels."
    },
    "commonLabels": {
      "$ref": "#/definitions/stringMap",
      "description": "Labels merged into every rendered resource."
    },
    "commonAnnotations": {
      "$ref": "#/definitions/stringMap",
      "description": "Annotations merged into every rendered resource."
    },
    "apiVersions": {
      "type": "object",
      "description": "Default API version overrides per supported cert-manager kind.",
      "additionalProperties": false,
      "properties": {
        "certificate": {
          "$ref": "#/definitions/nonEmptyString"
        },
        "certificateRequest": {
          "$ref": "#/definitions/nonEmptyString"
        },
        "challenge": {
          "$ref": "#/definitions/nonEmptyString"
        },
        "clusterIssuer": {
          "$ref": "#/definitions/nonEmptyString"
        },
        "issuer": {
          "$ref": "#/definitions/nonEmptyString"
        },
        "order": {
          "$ref": "#/definitions/nonEmptyString"
        }
      }
    },
    "certificates": {
      "$ref": "#/definitions/resourceList",
      "description": "List of Certificate resources."
    },
    "certificateRequests": {
      "$ref": "#/definitions/resourceList",
      "description": "List of CertificateRequest resources."
    },
    "challenges": {
      "$ref": "#/definitions/resourceList",
      "description": "List of Challenge resources."
    },
    "clusterIssuers": {
      "$ref": "#/definitions/resourceList",
      "description": "List of ClusterIssuer resources."
    },
    "issuers": {
      "$ref": "#/definitions/resourceList",
      "description": "List of Issuer resources."
    },
    "orders": {
      "$ref": "#/definitions/resourceList",
      "description": "List of Order resources."
    }
  },
  "definitions": {
    "nonEmptyString": {
      "type": "string",
      "minLength": 1
    },
    "stringMap": {
      "type": "object",
      "default": {},
      "propertyNames": {
        "type": "string",
        "minLength": 1
      },
      "additionalProperties": {
        "type": "string"
      }
    },
    "freeFormObject": {
      "type": "object",
      "description": "Arbitrary cert-manager payload passed through by the chart.",
      "additionalProperties": true
    },
    "resourceItem": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "name"
      ],
      "properties": {
        "name": {
          "$ref": "#/definitions/nonEmptyString",
          "description": "Resource name."
        },
        "namespace": {
          "$ref": "#/definitions/nonEmptyString",
          "description": "Namespace for namespaced resources. Ignored for cluster-scoped kinds."
        },
        "labels": {
          "$ref": "#/definitions/stringMap",
          "description": "Resource-specific labels."
        },
        "annotations": {
          "$ref": "#/definitions/stringMap",
          "description": "Resource-specific annotations."
        },
        "apiVersion": {
          "$ref": "#/definitions/nonEmptyString",
          "description": "Per-resource API version override."
        },
        "spec": {
          "$ref": "#/definitions/freeFormObject",
          "description": "Resource spec rendered as-is."
        },
        "status": {
          "$ref": "#/definitions/freeFormObject",
          "description": "Optional resource status rendered as-is."
        }
      }
    },
    "resourceList": {
      "type": "array",
      "default": [],
      "items": {
        "$ref": "#/definitions/resourceItem"
      }
    }
  }
}
